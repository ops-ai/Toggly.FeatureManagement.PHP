# Filter-parity golden fixtures

Vendored copy of `ops-ai/Toggly.FeatureManagement` `docs/filter-parity/fixtures`
so PHPUnit/CI can run without a sibling monorepo checkout.

When the canonical fixtures change, refresh this directory from that path
(or set `TOGGLY_FILTER_PARITY_FIXTURES` to point at an updated copy).
